import { StyleSheet } from "@/shared/components/rn";

export const styles = StyleSheet.create({
  amountSection: {
    alignItems: "center",
    gap: 4,
    paddingVertical: 8,
  },
  chipRow: {
    flexDirection: "row",
    flexWrap: "wrap",
    gap: 8,
  },
  inputGroup: {
    gap: 4,
  },
});
